services:
  flask_app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - DATABASE_PATH=/app/data/database.db
    volumes:
      - ./data:/app/data
